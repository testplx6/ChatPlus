# Ce dépôt

Le projet actif est **`game/`** — « Cendres & Protocole », une simulation
textuelle. Tout le travail se passe là : lis **`game/CLAUDE.md`** avant quoi que
ce soit.

Le reste du dépôt (`src/`, `public/`, `package.json` racine) est une ancienne
application React sans rapport. N'y touche pas, ne l'installe pas, ne lance pas
ses scripts.
